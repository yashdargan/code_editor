import APITest from '../../shared/api'
import './index.scss'

const Terminal:React.FC=()=>{
  return (
  <section className="terminal_container">
   <APITest />
    </section>
  )
} 
export default Terminal
